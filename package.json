{
  "name": "expense-mate",
  "scripts": {
    "start": "concurrently \"npm run frontend\" \"npm run backend\"",
    "frontend": "cd expense-mate && npm run dev",
    "backend": "cd backend && nodemon index.js"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "dependencies": {
    "bcrypt": "^6.0.0",
    "jspdf": "^3.0.1",
    "jspdf-autotable": "^5.0.2",
    "prop-types": "^15.8.1",
    "react-confirm-alert": "^3.0.6"
  }
}
